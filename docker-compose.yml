services:
  zookeeper:
    image: confluentinc/cp-zookeeper:7.5.0
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181
      ZOOKEEPER_TICK_TIME: 2000
    ports:
      - "2181:2181"
    healthcheck:
      test: ["CMD", "nc", "-z", "localhost", "2181"]
      interval: 10s
      timeout: 5s
      retries: 10

  kafka:
    image: confluentinc/cp-kafka:7.5.0
    depends_on:
      zookeeper:
        condition: service_healthy
    ports:
      - "9092:9092"
      - "29092:29092"
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT,PLAINTEXT_HOST:PLAINTEXT
      KAFKA_LISTENERS: PLAINTEXT://0.0.0.0:9092,PLAINTEXT_HOST://0.0.0.0:29092
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:9092,PLAINTEXT_HOST://localhost:29092
      KAFKA_INTER_BROKER_LISTENER_NAME: PLAINTEXT
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_NUM_PARTITIONS: 10
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: "true"
    healthcheck:
      test: ["CMD", "kafka-broker-api-versions", "--bootstrap-server", "localhost:9092"]
      interval: 10s
      timeout: 10s
      retries: 10

  clickhouse:
    image: clickhouse/clickhouse-server:24.8
    ports:
      - "8123:8123"
      - "9000:9000"
    environment:
      # Используем переменные из .env
      CLICKHOUSE_DB: ${CLICKHOUSE_DB}
      CLICKHOUSE_USER: ${CLICKHOUSE_USER}
      CLICKHOUSE_PASSWORD: ${CLICKHOUSE_PASSWORD}
    volumes:
      - clickhouse_data:/var/lib/clickhouse
    healthcheck:
      # Используем wget с авторизацией, если нужно, но ping обычно доступен всем
      # Для проверки здоровья лучше использовать простой ping, он не требует пароля
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:8123/ping"]
      interval: 10s
      timeout: 5s
      retries: 10

  producer:
    build:
      context: .
      dockerfile: producer/Dockerfile
    environment:
      KAFKA_BROKERS: ${KAFKA_BROKERS_INTERNAL}
      KAFKA_TOPIC: ${KAFKA_TOPIC}
      HTTP_ADDR: :${PRODUCER_PORT}
      GENERATION_MODE: regular
      SEND_MODE: batch
      PARTITION_STRATEGY: key
      REGULAR_RATE: 5
      BATCH_SIZE: 200
      BATCH_TIMEOUT_MS: 1500
      BURST_INTERVAL_SEC: 10
    depends_on:
      kafka:
        condition: service_healthy
    ports:
      - "${PRODUCER_PORT}:8080"
    restart: unless-stopped

  consumer:
    build:
      context: .
      dockerfile: consumer/Dockerfile
    environment:
      KAFKA_BROKERS: ${KAFKA_BROKERS_INTERNAL}
      KAFKA_TOPIC: ${KAFKA_TOPIC}
      KAFKA_GROUP_ID: page-views-consumer
      # Используем внутренний DSN для связи между контейнерами
      CLICKHOUSE_DSN: ${CLICKHOUSE_DSN_INTERNAL}
      CONSUMER_MODE: hybrid
      BATCH_SIZE: 1000
      FLUSH_INTERVAL_SEC: 5
      HTTP_ADDR: :${CONSUMER_PORT}
    depends_on:
      kafka:
        condition: service_healthy
      clickhouse:
        condition: service_healthy
    ports:
      - "${CONSUMER_PORT}:8081"
    restart: unless-stopped

volumes:
  clickhouse_data: